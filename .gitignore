# Ignore user secrets
examples/NotionPostTest/secrets.h

# PlatformIO build output
.pio/

# Arduino build extras
*.bin
*.elf
*.map

# Local credentials (excluded from Git)
# Note: SPIFFS is no longer used, credentials are in secrets.h

# Secrets header file (excluded from Git)
secrets.h
